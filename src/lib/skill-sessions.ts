export {
  defaultSkillId,
  ensureSkillSessions,
  syncActiveSession,
  loadSession,
  projectHasSkillWork,
  skillDisplayName,
  setSkillSessionName,
  clearInheritedStudyTitle,
} from '@shared/skill-sessions';

export type { SkillSession } from '@shared/skill-sessions';
